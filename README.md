# node-orm
Steps to use this code :
1. Clone this project on you local machine
2. Execute  [ node install ] inside the root folder ( which contains package.json )
3. Execute [ node index.js ]



Pre-requisites :
1.Need to have the MySQL server running
2.Update the MySQL URL and credentials, before executing the above code
